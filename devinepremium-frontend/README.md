# Devine Premium Frontend

Customer-facing website for service discovery, booking, login, payment follow-up, and booking history.

## Run

```bash
npm run dev
```

## Important Notes

- Existing marketing UI should stay unchanged unless explicitly requested.
- This app should only call the backend API through `NEXT_PUBLIC_API_BASE_URL`.
